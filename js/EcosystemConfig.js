define( function ( require ) {
	'use strict';

	var EcosystemConfig = {
		'viscosity' : 1000,
		'width'     : window.innerWidth,
		'depth'     : 1000,
		'height'    : window.innerHeight
	};

	return EcosystemConfig;
} );